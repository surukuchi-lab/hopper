# Configuration examples

YAML files for smoke tests, examples, and campaign-style simulations. Use these files as starting points for configuring trap fields, dynamics, signal synthesis, and output products.
